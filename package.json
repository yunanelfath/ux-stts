{
  "private": true,
  "scripts": {
    "start-install": "copy .env.example .env && php artisan key:generate && php artisan migrate && php artisan db:seed && npm install gulp bower && bower install && gulp --production && php artisan serve"
  },
  "devDependencies": {
    "bower": "^1.8.2",
    "gulp": "^3.8.8",
    "gulp-compass": "^2.1.0",
    "laravel-elixir-uglify": "^2.0.1"
  },
  "dependencies": {
    "bootstrap-sass": "^3.0.0",
    "gulp-copy": "^1.0.1",
    "laravel-elixir": "^4.0.0",
    "materialize-css": "^0.100.2"
  }
}
